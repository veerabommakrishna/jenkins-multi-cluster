apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: jenkins

helmCharts:
  - name: jenkins
    repo: https://charts.jenkins.io
    version: 4.9.0
    valuesFile: values.yaml
    releaseName: jenkins

resources:
  - namespace.yaml
